version: "2"
services:
  master:
    image: govindcnd/jenkinsmastersalve
    ports:
    - "80:8080"
    environment:
    - JAVA_OPTS=-Xmx1024m
    volumes:
    - jenkins-master:/var/jenkins_home
    - /home/ec2-user:/app
volumes:
  jenkins-master:

